export type Tokens = {
  accessToken: string;
  refreshToken: string;
};

export const isNew = true;

export const isNewKey = '@yuju-driver/isNew';

export const accessTokenKey = '@yuju-driver/token';
export const refreshTokenKey = '@yuju-driver/refresh-token';

export const isBanned = false;

export const accessToken = '';
export const refreshToken = '';
